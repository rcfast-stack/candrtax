Badge is an uppercase pill label for short status or category flags.

```jsx
<Badge tone="red">Tax Season</Badge>
<Badge tone="ice">Notary</Badge>
```

Use `red` sparingly — it inherits the CTA-only accent rule, so reserve it for genuinely time-sensitive flags (e.g. "Tax Season Hours").
